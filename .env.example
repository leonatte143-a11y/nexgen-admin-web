# NEXGEN Admin Web — copy to .env.local (dev) or set in Vercel build env (staging)
# VITE_API_BASE_URL=http://localhost:4000/api/v1

# Staging example (set in Vercel → Environment Variables before deploy):
VITE_API_BASE_URL=https://nexgen-backend-production.up.railway.app

# Optional — Google Maps (Geo + Live Monitor pages):
# VITE_GOOGLE_MAPS_KEY=
